name: swiftlint

on:
  push:
    branches:
      - "*"
  pull_request:
    branches:
    - "*"

jobs:
  build:
    runs-on: macOS-latest
    steps:
    - name: GitHub Action for SwiftLint
      uses: norio-nomura/action-swiftlint@3.1.0
